apiVersion: apps/v1
kind: Deployment
metadata:
  name: docs
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: docs
  template:
    metadata:
      labels:
        app.kubernetes.io/name: docs
        istio.io/dataplane-mode: ambient
    spec:
      containers:
        - image: registry.registry.svc.cluster.local/khuedoan/homelab:b7d8037fa8769615d284fc0adc118ee2b4625266
          name: docs
          ports:
            - containerPort: 80
              name: http
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 3
            periodSeconds: 3
